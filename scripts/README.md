# scripts/

This directory is reserved for future automation scripts.

## Planned scripts (not in v1)

- `crawl_kppp.py` — would fetch the latest BBMP/BWSSB/BESCOM tenders from kppp.karnataka.gov.in and regenerate `data/works.json`.

This is part of the v1.1 roadmap (GitHub Actions weekly cron). For now, `data/works.json` is a hand-curated sample and a prominent banner in the UI says so.
